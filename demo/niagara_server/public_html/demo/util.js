function max(x, y) {
	return Math.max(x, y);
}

function min(x, y) {
    return Math.min(x, y);
}

function absdiff(x, y) {
    return Math.abs(x - y);
}

function ceil(x) {
    return Math.ceil(x);
}

function findElement(id) {
	return document.getElementById(id);
}